# ============================================================================
# StemTube Web - Environment Configuration Example
# ============================================================================
# Copy this file to .env and fill in your actual values
#
# IMPORTANT: Never commit .env to version control!
# The .env file should be added to .gitignore
# ============================================================================

# ----------------------------------------------------------------------------
# Flask Security Configuration (REQUIRED)
# ----------------------------------------------------------------------------

# Flask secret key for session signing and CSRF protection
# Generate a secure random key with: python -c "import secrets; print(secrets.token_hex(32))"
# WARNING: Changing this will invalidate all existing sessions
FLASK_SECRET_KEY=CHANGE_ME_GENERATE_WITH_python_-c_"import secrets; print(secrets.token_hex(32))"

# ----------------------------------------------------------------------------
# ngrok Configuration (Optional but Recommended)
# ----------------------------------------------------------------------------

# ngrok custom URL for tunneling (required for mobile/HTTPS features)
# Leave empty to use random ngrok URL
# Format: https://your-subdomain.ngrok-free.app
# Get your custom URL from: https://dashboard.ngrok.com/cloud-edge/domains
NGROK_URL=

# ----------------------------------------------------------------------------
# Application Settings (Optional)
# ----------------------------------------------------------------------------

# Flask environment (development, production)
FLASK_ENV=production

# Flask debug mode (0 = off, 1 = on)
# WARNING: Never enable debug mode in production!
FLASK_DEBUG=0

# Application host (default: 0.0.0.0)
FLASK_HOST=0.0.0.0

# Application port (default: 5011)
# Override by setting STEMTUBE_PORT or PORT environment variable
# STEMTUBE_PORT=5011

# ----------------------------------------------------------------------------
# Database Configuration (Optional)
# ----------------------------------------------------------------------------

# SQLite database path (default: stemtubes.db in project root)
# DATABASE_PATH=stemtubes.db

# ----------------------------------------------------------------------------
# GPU/Processing Configuration (Optional)
# ----------------------------------------------------------------------------

# Use GPU for stem extraction (default: auto-detect)
# USE_GPU=1

# Use GPU for lyrics transcription (default: auto-detect)
# USE_GPU_LYRICS=1

# Maximum concurrent downloads (default: 3)
# MAX_CONCURRENT_DOWNLOADS=3

# Maximum concurrent extractions (default: 1)
# MAX_CONCURRENT_EXTRACTIONS=1

# ----------------------------------------------------------------------------
# Advanced Settings (Optional)
# ----------------------------------------------------------------------------

# Demucs model for stem extraction (default: htdemucs)
# Options: htdemucs, htdemucs_ft, htdemucs_6s, mdx_extra
# DEFAULT_STEM_MODEL=htdemucs

# Whisper model for lyrics transcription (default: medium)
# Options: tiny, base, small, medium, large, large-v3
# WHISPER_MODEL_SIZE=medium

# Enable silent stem detection (default: true)
# ENABLE_SILENT_STEM_DETECTION=true

# Silent stem threshold in dB (default: -40.0)
# SILENT_STEM_THRESHOLD_DB=-40.0

# ----------------------------------------------------------------------------
# Logging Configuration (Optional)
# ----------------------------------------------------------------------------

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Log file path (default: logs/app.log)
# LOG_FILE=logs/app.log

# Enable request logging (default: true)
# ENABLE_REQUEST_LOGGING=true

# ----------------------------------------------------------------------------
# Security Best Practices
# ----------------------------------------------------------------------------
#
# 1. Generate strong random secrets:
#    python -c "import secrets; print(secrets.token_hex(32))"
#
# 2. Restrict file permissions:
#    chmod 600 .env
#
# 3. Never commit .env to git:
#    Add .env to .gitignore
#
# 4. Use different secrets for dev/staging/production
#
# 5. Rotate secrets regularly in production
#
# ============================================================================
